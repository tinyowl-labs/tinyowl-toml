# Minimal tinyowl.toml — just source_id and name columns.
# Suitable for a simple list of records with no geometry, dates, or relations.

[import]
entity_type = "record"
visibility = "group"

[[column]]
source = "CatalogNumber"
target = "source_id"

[[column]]
source = "Title"
target = "name"
